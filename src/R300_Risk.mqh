#ifndef R300_RISK_MQH
#define R300_RISK_MQH
#include "R300_Config.mqh"

// TODO: Position sizing, SL calculation and maximum risk protection.
// No risk approval or calculated size is provided until rules are specified.
ENUM_R300_STATUS R300_EvaluateRisk()
  {
   return(R300_NOT_IMPLEMENTED);
  }
#endif
