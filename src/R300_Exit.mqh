#ifndef R300_EXIT_MQH
#define R300_EXIT_MQH
#include "R300_Config.mqh"

// TODO: Take Profit, Break Even, Trailing Stop, Structure Exit, Partial Close.
// Placeholder only; never modifies existing positions.
ENUM_R300_STATUS R300_EvaluateExit()
  {
   return(R300_NOT_IMPLEMENTED);
  }
#endif
